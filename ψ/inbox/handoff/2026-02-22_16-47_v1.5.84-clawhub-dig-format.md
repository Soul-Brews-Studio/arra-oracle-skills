# Handoff: v1.5.84 — ClawHub Build + Skill Fixes

**Date**: 2026-02-22 16:47
**Context**: ~70%

## What We Did

- **Implemented `scripts/clawhub-build.ts`** — rsync each skill to `dist/clawhub/{name}/`, injects `version:` + `publisher: soul-brews-studio` into SKILL.md frontmatter, excludes ψ/, node_modules, tests. `bun run clawhub` builds all 27 skills.
- **Fixed #44** — Removed `disable-model-invocation: true` from `/speak` and `/philosophy`. Both skills now invocable via Skill tool.
- **Released v1.5.84** — compiled, installed to 7 agents, committed, tagged, pushed.
- **Deep /trace --dig session** — searched all Claude Code sessions for "names suggested" by user. Found the Feb 20 research session (`d8966e01`) where deep research on ClawHub/OpenClaw/skills.sh ecosystem was done. Names question remains unresolved (likely from non-Claude-Code tool).

## Pending

- [ ] **Issue #45** — `dig.py` timeline format: show `HH:MM–HH:MM (Nm)` instead of just start + duration
- [ ] **Issue #45 comment** — Add gap rows (`· · ·`) when >30 min gap between sessions
- [ ] **Issue #43** — `/watch` + `/gemini`: MQTT chat action fails + yt-dlp dependency missing
- [ ] **Issue #42** — `/rrr --dig`: path encoding breaks on dots + missing worktree awareness
- [ ] **Issue #41** — OracleNet markdown rendering
- [ ] **Issue #40** — OracleNet mentions inbox endpoint
- [ ] Publish oracle skills to ClawHub (build script ready, need `clawhub login` + `clawhub publish`)

## Next Session

- [ ] Fix `dig.py` for issue #45 — format `startGMT7–endGMT7 (Nm)` + gap rows with `· · ·`
- [ ] Test dig output: `bun run src/cli/index.ts install -y -g --skill trace` then `/trace --dig`
- [ ] Close #45 after fix

## Key Files

- `scripts/clawhub-build.ts` — new ClawHub build script
- `~/.claude/skills/trace/scripts/dig.py` — session timeline parser (needs #45 fixes)
- `src/skills/speak/SKILL.md` — fixed (no more disable-model-invocation)
- `src/skills/philosophy/SKILL.md` — fixed (no more disable-model-invocation)
