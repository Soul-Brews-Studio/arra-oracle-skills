---
description: v3.3.0-alpha.10 | Transfer work to another Oracle — write handoff, wake target, send context. Use when user says "handover", "hand over", "pass to", "transfer to", or wants to finish their part and pass work to another Oracle. Do NOT trigger for just messaging (use /talk-to or /tell), just spawning (use /wake), or just saving context (use /forward).
argument-hint: "<oracle-name> <instruction>"
---

# /handover

Execute the `handover` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "handover"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file at this exact path: `~/.claude/skills/handover/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

**WARNING**: Do NOT use Glob, find, or search for this skill. The path above is the ONLY correct location. Other files with "handover" in the name are NOT this skill.

---
*🧬 Nat Weerawan × Oracle · Symbiotic Intelligence · v3.3.0-alpha.10*
*Digitized from Nat Weerawan's brain — thousands of hours working alongside AI, captured as code*
